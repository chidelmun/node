language: cpp

compiler:
  - gcc

script: make && make test
notifications:
  email: false
  irc:
    channels:
    - chat.freenode.net#ublab
    template:
    - "%{repository}/%{branch} (%{commit} - %{author}): %{build_url}: %{message}"